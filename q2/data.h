#define MAX_LINE_LEN 100



#define INPUT_PROMPT "\nprogram input: a C like text, maximum of %d chars long per line\n\n",MAX_LINE_LEN
#define OUTPUT_PROMPT "program output: tests for brackets balance, for ( { [ type brackets\nprints the lines of the original text with a comment per line noting if the line is balanced.\nat the end of the program prints out a summary whether program itself is balanced.\n i.e [()] is balanced, (}) is not balanced.\n******************************\n\n"
#define REQUEST_PROMPT "Please enter  text:\n\n"

#define current line[i]
#define next line[i+1]

